# Vite forwards /api/* to Django. Must match the port where you run:
#   cd SalesBooster_AI/django_backend && python manage.py runserver
# (default is 8000). If another app uses 8000, run Django on 8001 and set:
#   API_PROXY_TARGET=http://127.0.0.1:8001
API_PROXY_TARGET=http://127.0.0.1:8000
